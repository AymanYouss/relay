global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: relay
    metrics_path: /metrics
    static_configs:
      - targets: ["relay:9090"]
        labels:
          service: relay
